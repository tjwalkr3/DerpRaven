apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: derp-db-pvc-new2
  namespace: ${NAMESPACE}
spec:
  accessModes:
  - ReadWriteOnce
  #- ReadWriteMany
  resources:
    requests:
      storage: 1Gi
